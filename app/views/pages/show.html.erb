<%= content_tag(:div, :class => "page-header") do %>
  <%= render_page_title(resource.title) %>
<% end if resource.show_title_in_page %>

<div class="container">
  <div class="row">
    <div class="col-sm-12">
      <%= render_copy_text(resource.body) %>
    </div>
  </div>
</div>
